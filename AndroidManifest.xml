<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
      package="com.jittr.android.test"
      android:versionCode="1"
      android:versionName="1.0">
    <application android:icon="@drawable/icon" android:label="@string/app_name">

    <uses-library android:name="android.test.runner" />
    </application>
    <uses-sdk android:minSdkVersion="7" />
     <!--
        This declares that this application uses the instrumentation test runner targeting
        the package of com.android.example.spinner.  To run the tests use the command:
        "adb shell am instrument -w \
         com.jittr.android.test/android.test.InstrumentationTestRunner"
    -->
    <instrumentation android:targetPackage="com.jittr.android" 
                     android:name="android.test.InstrumentationTestRunner"
                     android:label="GameOn Tests" />
</manifest> 